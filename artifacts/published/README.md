# Published Artifacts

This directory is the only `artifacts/` subtree intended to be committed.

Rules:
- keep only curated outputs worth sharing or reproducing
- do not copy raw training runs, logs, replay data, or transient checkpoints here
- every published model should have a sibling metadata JSON file
- file names should stay stable and descriptive

Current contents:
- `models/`: curated checkpoints selected for sharing
